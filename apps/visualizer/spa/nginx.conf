server {
  listen 80;

  location / {
    root   /usr/share/nginx/html;
    index  index.html index.pages.html;
    try_files $uri $uri/ @rewrite; # Needed for SPA routing
  }
  
  location @rewrite {
    rewrite ^/pages/(.+)$ /index.pages.html last;
    rewrite ^(.+)$ /index.html last;
  }
}
