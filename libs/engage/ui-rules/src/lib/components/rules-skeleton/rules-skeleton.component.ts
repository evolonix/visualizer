import { Component } from '@angular/core';

@Component({
  selector: 'dgs-rules-skeleton',
  templateUrl: './rules-skeleton.component.html',
})
export class RulesSkeletonComponent {
  items = new Array(5);
}
