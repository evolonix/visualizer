# engage-ui-common

This library contains common UI providers, like the `AuthenticationGuard` for
routing.

It also contains the login page and home page components (and their routing
config).

## Running unit tests

Run `nx test engage-ui-common` to execute the unit tests.
