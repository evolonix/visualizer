## Setup

- Open Github and go to developer settings.
- Create a token that has `repo` scope.
- Add SSO for the Degreed org to that token.
- Open a shell and define `GITHUB_TOKEN` with a value of the token created above.
- Run Terraform commands.
